package main

import "fmt"

func main() {
	a := 10
	b := "Golang"
	c := 4.17
	d := true

	fmt.Printf("a := %v (%T)\n", a, a)
	fmt.Printf("b := %v (%T)\n", b, b)
	fmt.Printf("c := %v (%T)\n", c, c)
	fmt.Printf("d := %v (%T)\n", d, d)
}
